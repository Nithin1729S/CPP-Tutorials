#include<stdio.h>
#include<stdlib.h>

typedef struct point
{
    int x;
    int y;
}Point;

int main()
{
    Point pointsArray[5];
    int i;
    printf("Enter the first point x coordinate: ");
    scanf("%d",&pointsArray[0].x);
    printf("Enter the first point y coordinate: ");
    scanf("%d",&pointsArray[0].y);

    for(i=0;i<5;i++)
    {
        
    }
}